

export class Tombstone {
  constructor(x, y, height, width, depth, text) {
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
    this.depth = depth;

    make();
  }

  make() {
    for (let i=0; i < 10; i++) {
    }
  }

  geometry() {
  }
}
