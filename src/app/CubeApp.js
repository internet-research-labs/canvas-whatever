import App from './App.js';

var THREE = require('THREE');

export default class CubeApp extends App {
  constructor(params) {
    super(params);
  }

  setup(params) {
  }

  update(params) {
  }

  draw() {
  }
}
