import App from './App.js';
import Line from '../Line.js';
import TriGrid from '../TriGrid.js';

export default class TriGridApp extends App {
  constructor(params) {
    super(params);
  }

  setup() {
  }

  update(params) {
  }

  clear() {
  }

  draw() {
  }
}

